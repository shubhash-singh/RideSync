package com.ragnar.RideSync.domain.repository

/**
 * Base repository interface.
 * All domain-layer repository interfaces should extend this marker interface
 * to maintain a consistent contract across the project.
 */
interface BaseRepository
